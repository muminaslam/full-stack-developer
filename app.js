// Referene Error
console.log(name);
// the console will throw an error that the varaiable "name" not defined, and it is called as reference error

// Undefined
var age
console.log(age);
// In here console will popup the variable age is 'undefined', becaue of no values are assigned to the variable

// Null
var color = null;
console.log(color);
// Now console will throw the datatype of null to the output.

/*
    "Undefined" is the error thrown by the JavaScript engine to the user for clarification of that a variable is not defined.

    "NUll" is the error where developers send to the users that a particular variable is a datatype of "NULL"
 */