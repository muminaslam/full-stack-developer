// Variable Types

var age = 21;
age = 22;
console.log(age);
// In var variable type a valua can be reassinged serveral times.

const idNo = 12545346;

idNo = 34534534;
console.log(IdNo);

// IN const, a varaiable's value can be assigned only one time, can't the variable value after the declaration.